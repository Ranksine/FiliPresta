{
  "name": "filipresta",
  "version": "1.0.0",
  "description": "Gestión de préstamos Filemón",
  "private": true,
  "main": "index.js",
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/api",
    "apps/client"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "electron:dev": "wait-on http://localhost:3000 && electron ./electron.js",
    "electron:build": "npm run build && electron-builder"
  },
  "keywords": [],
  "author": "Ranksine",
  "license": "ISC",
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "concurrently": "^9.2.0",
    "electron": "^37.2.5",
    "turbo": "^2.5.5",
    "wait-on": "^8.0.4"
  },
  "dependencies": {
    "dotenv": "^17.2.1"
  }
}
